export type Robot = {
  robotId?: string;
  serialNumber: number;
  name: string;
  battery: number;
  mode: string;
  activity: string;
  state: string;
  errorCode: number;
  clientId: string;
  modelId: string;
};
